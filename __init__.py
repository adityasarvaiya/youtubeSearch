# All templates are stored in this folder, just replace them as needed, all of them setup for django-3.0.5

# reqs - Folder contains all requirements(Move it to top level inside twd)
# settings_class_based - Folder contains settings as per django_configurations(Move it inside project directory in twd)
# settings_normal - Folder contains settings in default django style(Move it inside project directory in twd)
# base_app - Folder contains all required files of base app(Move it inside apps folder in twd or project directory in twd)
# wsgi_with_django_configurations - wsgi file to be used with class based settings
# wsgi_normal
# asgi_with_django_configurations
# asgi_normal